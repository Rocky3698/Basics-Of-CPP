#include <bits/stdc++.h>
using namespace std;
#define endl "\n"
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int a;
    while (cin >> a)
    {
        // if you take input inside the loop then it will take input untill it get a end of file
    }

    return 0;
}
